[project]
name = "sentral-ops-private-sop-assistant-backend"
version = "0.1.0"
description = "FastAPI RAG backend for the Sentral Ops Private SOP Assistant demo."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
